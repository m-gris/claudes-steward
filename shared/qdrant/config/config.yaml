storage:
  # Overridden by QDRANT__STORAGE__STORAGE_PATH in justfile
  # Default used only if launched without the justfile
  storage_path: ./storage

service:
  host: 127.0.0.1
  http_port: 6333
  grpc_port: 6334

telemetry_disabled: true
